# Зикр

Ўзбек тилида (кирилл) зикр ва дуолар тўплами. Ягона статик HTML файл.

## Бу файл ҳақида

Фойдаланувчи буюрган: **ҳар сессияда ўзгариш қилинганда, шу файлни ҳам янгилаш**. Янги файл, конвенция, workflow ўзгариши, архитектура қарори — буларни CLAUDE.md-да акс эттириш. Майда матн тузатишлари кирмайди. Янгилашни алоҳида commit-да эмас, шу ўзгариш билан бирга commit қилиш.

## Тузилиш

- `index.html` — бутун лойиҳа (HTML + inline CSS + inline JS). Build йўқ.
- `favicon.svg`, `favicon.png`, `apple-touch-icon.png` — иконкалар.
- `manifest.json` — PWA манифести (ўрнатиш учун).
- `sw.js` — service worker (офлайн, network-first).
- `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` — PWA иконкалари.
- Бошқа файл йўқ.

## PWA (ўрнатиладиган илова)

- Илова Android/десктоп/iOS-да "Бош экранга қўшиш" орқали ўрнатилади (standalone режим).
- Service worker стратегияси: **network-first**. Онлайн бўлганда доим тармоқдан янги версияни беради — фойдаланувчи кэш тозалаши шарт эмас. Офлайнда охирги кэшланган версия ишлайди.
- Файлни ўзгартирганда фойдаланувчилар навбатдаги киришда автомат янги версияни олади. Мажбуран кэш янгилаш керак бўлса, `sw.js`-даги `CACHE` номини бумп қилиш (`zikr-v1` → `zikr-v2`) — `activate` эски кэшларни автомат ўчиради.
- PWA иконкалари Cormorant Garamond шрифти билан `favicon.svg`-дан генерация қилинган (cairosvg). Қайта генерация керак бўлса, шрифтни ўрнатиб (Google Fonts), кичик python скрипт билан 192/512px ва maskable вариантини чизиш.
- Ўрнатиш banner-и (`.install-banner`): ўрнатилмаган Chrome/Edge/Android фойдаланувчиларга экран остида чиройли таклиф. `beforeinstallprompt` орқали ишлайди. Ёпилса, `localStorage` (`zikr_install_dismissed_at`) 14 кун кўрсатмайди. iOS Safari `beforeinstallprompt`-ни қўлламайди — у ерда banner чиқмайди (нормал). Banner матни ҳам алифбо алмаштиргичига бўйсунади.

## Хостинг

GitHub Pages, `main` бранчдан хизмат қилади. Merge-дан 1–2 дақиқа кейин жонли.

## Workflow

- `main` бранч protected — тўғридан-тўғри push мумкин эмас.
- Ишни `claude/review-and-feedback-W7BYo` (ёки бошқа feature) бранчда қилиш.
- Commit → push → PR очиш → фойдаланувчи GitHub-да merge қилади.

## Контент тузилиши

Беш гуруҳ (`.group` → `.group-header` + `.group-body`):
1. Дуо боши ва охирида айтиладиган илтижолар
2. Эрталабки зикрлар
3. Кечқурунги зикрлар
4. Жума куни дуолари
5. Руқия

Ҳар бирида `.card` элементлар. Кўпинча `.card-arabic` (арабча матн) + `.card-footer` (`.card-count` манба/такрор сони). Жума ва дуо боши гуруҳларида қўшимча `.card-number` бор.

Бир оят/дуо мантиқан бир бутун бўлса — битта картага қўйилади (масалан Наҳл: 68-69). Кетма-кет оятлар орасига `۝` ажратгичи қўйилади.

## Дизайн қарорлари

- Палитра CSS variables-да (`:root` ва `[data-theme="dark"]`).
- Шрифтлар: Cormorant Garamond (logo, рақамлар), Scheherazade New (арабча), PT Serif (асосий матн).
- Theme: `localStorage`-да, FOUC олдини олиш учун inline скрипт `<head>`-да эмас, `<body>` бошида.
- Шрифт катталаштириш А+/А− — фақат арабча матнга таъсир қилади (`--base-font-size` орқали).
- Бошқарув тугмалари (тема, А+/А−, алифбо) юқори-ўнгдаги битта созламалар тугмаси (`.settings`, шестерёнка SVG) ичида яширин панелда. Панель ташқарига босиш ёки Escape билан ёпилади.
- Алифбо (кирилл ↔ лотин): контент **кириллда** ёзилади, лотин варианти `translit()` функцияси билан автомат генерация қилинади (ўзбек кирилл→лотин транслитерацияси). Танлов `localStorage`-да (`zikr-script`), default — кирилл. `setScript()` барча кирилл матн node-ларини алмаштиради; арабча матнда кирилл ҳарфи йўқлиги учун у ўзгармай қолади (қўлда дубль контент сақланмайди).

## Қоидалар

- Эмодзи қўшмаслик (ҳеч қаерда — UI, commit, доc).
- Ўлик код қўшмаслик: декларация ишлатилмаса, ё чақириш, ё ўчириш.
- Янги хусусият қўшмасдан олдин фойдаланувчидан сўраш.

## Тил

Фойдаланувчи ўзбек кирилл ёзади. Жавоблар ҳам ўзбек кирилл бўлсин. Commit message ва код изоҳлари инглизча бўлиши мумкин (стандарт амалиёт).
